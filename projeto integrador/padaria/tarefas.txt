═══════════════════════════════════════════════════════════════════════════════
                    PROJETO INTEGRADOR - PADARIA API REST
                    Django REST Framework | Python
                    Semana: 06/12/2025
═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│  ATIVIDADE 01: Configuração e Gestão de Usuários                            │
│  Objetivo: Implementar CRUD completo de usuários com autenticação customizada│
└─────────────────────────────────────────────────────────────────────────────┘

📋 Contexto:
   - Foco apenas no cadastro e manutenção de usuários
   - Sem camada de segurança (Token/JWT) neste momento
   - Utilizar AbstractUser para personalização do modelo de usuário

📦 Passo 1.1: Configuração do App e Modelo de Usuário
   
   ✓ Criar app 'users':
     → python manage.py startapp users
   
   ✓ Registrar app:
     → Adicionar 'users' em INSTALLED_APPS no settings.py
   
   ✓ Criar modelo customizado:
     → Arquivo: users/models.py
     → Classe: User(AbstractUser)
     → Import: from django.contrib.auth.models import AbstractUser
   
   ✓ Configurar modelo de autenticação:
     → Arquivo: settings.py
     → Adicionar: AUTH_USER_MODEL = 'users.User'
   
   ✓ Executar migrações:
     → python manage.py makemigrations
     → python manage.py migrate

───────────────────────────────────────────────────────────────────────────────

📦 Passo 1.2: Implementação da API REST de Usuários (CRUD)

   ✓ Criar Serializer:
     → Arquivo: users/serializers.py
     → Classe: UserSerializer(serializers.ModelSerializer)
     → Fields: ['id', 'first_name', 'username', 'password']
     
     ⚠️ IMPORTANTE: Sobrescrever método create():
        def create(self, validated_data):
            password = validated_data.pop('password')
            user = User.objects.create(**validated_data)
            user.set_password(password)  # Criptografa a senha
            user.save()
            return user
   
   ✓ Criar ViewSet:
     → Arquivo: users/views.py
     → Classe: UserViewSet(viewsets.ModelViewSet)
     → Atributos:
       • queryset = User.objects.all()
       • serializer_class = UserSerializer
   
   ✓ Configurar Rotas:
     → Arquivo: config/urls.py (ou urls.py principal)
     → Importar: from rest_framework.routers import DefaultRouter
     → Criar: router = DefaultRouter()
     → Registrar: router.register(r'users', UserViewSet, basename='users')
     → Incluir: path('api/', include(router.urls))
   
   ✓ Testar Endpoints:
     → POST   /api/users/     (Criar usuário)
     → GET    /api/users/     (Listar usuários)
     → GET    /api/users/{id}/ (Detalhar usuário)
     → PUT    /api/users/{id}/ (Atualizar usuário)
     → DELETE /api/users/{id}/ (Deletar usuário)
     → Ferramentas: Bruno/ Postman / Insomnia / httpie

═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│  ATIVIDADE 02: Gestão de Fornecedores                                       │
│  Objetivo: Criar API REST para gerenciar fornecedores do sistema            │
└─────────────────────────────────────────────────────────────────────────────┘

📋 Contexto:
   - Fornecedores são entidades independentes
   - Não possuem relacionamento obrigatório com outras entidades neste momento

📦 Passo 2.1: Configuração do App e Modelo de Fornecedor

   ✓ Criar app 'products' ou 'core':
     → python manage.py startapp products
     → (Ou usar 'core' se preferir estrutura centralizada)
   
   ✓ Registrar app:
     → Adicionar 'products' em INSTALLED_APPS no settings.py
   
   ✓ Criar modelo Supplier:
     → Arquivo: products/models.py
     → Campos:
       • nome: CharField(max_length=100)
       • email: EmailField()
       • telefone: CharField(max_length=20)
       • cnpj: CharField(max_length=18, unique=True)
     → Adicionar: __str__() e Meta (verbose_name, verbose_name_plural)
   
   ✓ Executar migrações:
     → python manage.py makemigrations
     → python manage.py migrate

───────────────────────────────────────────────────────────────────────────────

📦 Passo 2.2: Implementação da API REST de Fornecedores (CRUD)

   ✓ Criar Serializer:
     → Arquivo: products/serializers.py
     → Classe: SupplierSerializer(serializers.ModelSerializer)
     → Meta: model = Supplier, fields = '__all__'
   
   ✓ Criar ViewSet:
     → Arquivo: products/views.py
     → Classe: SupplierViewSet(viewsets.ModelViewSet)
     → Atributos:
       • queryset = Supplier.objects.all()
       • serializer_class = SupplierSerializer
   
   ✓ Configurar Rotas:
     → Arquivo: config/urls.py
     → Registrar: router.register(r'suppliers', SupplierViewSet, basename='suppliers')
   
   ✓ Testar Endpoints:
     → POST   /api/suppliers/     (Criar fornecedor)
     → GET    /api/suppliers/     (Listar fornecedores)
     → GET    /api/suppliers/{id}/ (Detalhar fornecedor)
     → PUT    /api/suppliers/{id}/ (Atualizar fornecedor)
     → DELETE /api/suppliers/{id}/ (Deletar fornecedor)

═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│  ATIVIDADE 03: Gestão de Produtos                                           │
│  Objetivo: Criar API REST para gerenciar produtos com relacionamento        │
└─────────────────────────────────────────────────────────────────────────────┘

📋 Contexto:
   - Produtos possuem relacionamento com Fornecedores (ForeignKey)
   - Produtos possuem campos de controle de estoque e validade

📦 Passo 3.1: Configuração do Modelo de Produto

   ✓ Criar modelo Product:
     → Arquivo: products/models.py (mesmo app de Supplier)
     → Campos obrigatórios:
       • name: CharField(max_length=200)
       • supplier: ForeignKey(Supplier, on_delete=models.CASCADE, related_name='products')
       • qtd_atual: IntegerField(default=0)
       • qtd_minima: IntegerField(default=0)
       • validade: DateField()
       • lote: CharField(max_length=50)
     → Adicionar: __str__() e Meta (verbose_name, verbose_name_plural)
   
   ✓ Executar migrações:
     → python manage.py makemigrations
     → python manage.py migrate

───────────────────────────────────────────────────────────────────────────────

📦 Passo 3.2: Implementação da API REST de Produtos (CRUD)

   ✓ Criar Serializer:
     → Arquivo: products/serializers.py
     → Classe: ProductSerializer(serializers.ModelSerializer)
     → Configuração:
       • Na escrita: aceitar apenas o ID do fornecedor (supplier_id)
       • Na leitura: opcionalmente retornar dados do fornecedor aninhados
       • Opções:
         a) Usar depth = 1 para serialização simples aninhada
         b) Criar SupplierSerializer aninhado para mais controle
     
     → Exemplo com depth:
       class Meta:
           model = Product
           fields = '__all__'
           depth = 1  # Serializa objetos relacionados até 1 nível
   
   ✓ Criar ViewSet:
     → Arquivo: products/views.py
     → Classe: ProductViewSet(viewsets.ModelViewSet)
     → Atributos:
       • queryset = Product.objects.select_related('supplier').all()
       • serializer_class = ProductSerializer
     → Observação: select_related() otimiza consultas com ForeignKey
   
   ✓ Configurar Rotas:
     → Arquivo: config/urls.py
     → Registrar: router.register(r'products', ProductViewSet, basename='products')
   
   ✓ Testar Endpoints:
     → POST   /api/products/     (Criar produto)
     → GET    /api/products/     (Listar produtos)
     → GET    /api/products/{id}/ (Detalhar produto)
     → PUT    /api/products/{id}/ (Atualizar produto)
     → DELETE /api/products/{id}/ (Deletar produto)

═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│  ATIVIDADE 04: Sistema de Filtros e Busca                                   │
│  Objetivo: Implementar filtros avançados para busca de produtos             │
└─────────────────────────────────────────────────────────────────────────────┘

📋 Contexto:
   - Implementar filtros para facilitar consultas de produtos
   - Permitir busca por validade, fornecedor e nome do produto

📦 Passo 4.1: Instalação e Configuração do django-filter

   ✓ Instalar biblioteca:
     → pip install django-filter
   
   ✓ Registrar no settings.py:
     → Adicionar 'django_filters' em INSTALLED_APPS
     → Adicionar configuração:
       REST_FRAMEWORK = {
           'DEFAULT_FILTER_BACKENDS': [
               'django_filters.rest_framework.DjangoFilterBackend',
           ],
       }

───────────────────────────────────────────────────────────────────────────────

📦 Passo 4.2: Implementação de Filtros na ViewSet

   ✓ Configurar filtros:
     → Arquivo: products/views.py
     → Adicionar import: from django_filters.rest_framework import DjangoFilterBackend
     → Na classe ProductViewSet, adicionar:
       • filter_backends = [DjangoFilterBackend]
       • filterset_fields = ['validade', 'supplier', 'name']
       • (Ou usar search_fields para busca parcial no nome)
   
   ✓ Configuração completa exemplo:
     class ProductViewSet(viewsets.ModelViewSet):
         queryset = Product.objects.select_related('supplier').all()
         serializer_class = ProductSerializer
         filter_backends = [DjangoFilterBackend]
         filterset_fields = ['validade', 'supplier', 'name']
         # Opcional: busca parcial no nome
         # search_fields = ['name']
         # search_fields = ['name', 'lote']
   
   ✓ Testar Filtros:
     → GET /api/products/?validade=2025-12-31
     → GET /api/products/?supplier=1
     → GET /api/products/?name=Pão
     → GET /api/products/?validade=2025-12-31&supplier=1&name=Pão

═══════════════════════════════════════════════════════════════════════════════

┌─────────────────────────────────────────────────────────────────────────────┐
│  ATIVIDADE 05: Validações e Melhorias                                       │
│  Objetivo: Adicionar validações de negócio e melhorar a qualidade do código │
└─────────────────────────────────────────────────────────────────────────────┘

📋 Contexto:
   - Implementar validações nos serializers
   - Adicionar mensagens de erro personalizadas
   - Melhorar tratamento de erros

📦 Passo 5.1: Validações no ProductSerializer

   ✓ Validar quantidade mínima:
     → Garantir que qtd_atual >= 0
     → Garantir que qtd_minima >= 0
     → Opcional: alertar se qtd_atual < qtd_minima
   
   ✓ Validar data de validade:
     → Garantir que validade seja data futura
     → Opcional: alertar produtos próximos do vencimento
   
   ✓ Validar CNPJ (SupplierSerializer):
     → Criar método de validação para CNPJ
     → Garantir formato correto (opcional: usar biblioteca de validação)

───────────────────────────────────────────────────────────────────────────────

📦 Passo 5.2: Melhorias Gerais

   ✓ Adicionar paginação:
     → Configurar paginação global no settings.py
     → Ou configurar por ViewSet
   
   ✓ Documentação automática:
     → Instalar: pip install drf-yasg ou drf-spectacular
     → Configurar Swagger/OpenAPI para documentação automática
   
   ✓ Ordenação:
     → Adicionar ordering_fields nas ViewSets
     → Exemplo: ordering_fields = ['name', 'validade', 'qtd_atual']
   
   ✓ Testes:
     → Criar testes unitários para serializers
     → Criar testes de integração para ViewSets
     → Arquivo: products/tests.py

═══════════════════════════════════════════════════════════════════════════════

📌 DEPENDÊNCIAS ENTRE ATIVIDADES:

   Atividade 01 → Atividade 02 (usuários podem ser referenciados no futuro)
   Atividade 02 → Atividade 03 (produtos dependem de fornecedores)
   Atividade 03 → Atividade 04 (filtros aplicados em produtos)
   Atividade 04 → Atividade 05 (validações melhoram filtros existentes)

═══════════════════════════════════════════════════════════════════════════════
